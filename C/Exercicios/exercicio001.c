#include <stdio.h>
void main(){
//Usam-se as chaves { } para delimitar o que está incluso no corpo dessa função.
//dentro da função main(), inserimos as instruções que serão executadas. 
//comentario, isso não faz nada	
//printf("Hello World!");
//Perceba que a string (cadeia de caracteres) está entre aspas, uma vez que servem para delimitá-la.
// \n faz a quebra de linha

int a, b, c;
a = 1;
b = a + 3;
c = a;
printf("b = %d e c = %d.\n", b, c);
}
